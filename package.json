{
  "name": "jsonx-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "core",
    "mini",
    "tools"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "build": "npm run -ws build",
    "test": "npm run -ws test"
  },
  "dependencies": {
    "ts-jest": "^29.4.6"
  }
}
